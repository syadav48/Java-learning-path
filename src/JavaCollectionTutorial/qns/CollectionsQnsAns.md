Got you — here is **Option C: Pure Practice Workbook**.

No theory.
No explanations.
Only **high-quality questions** to train your Collections, List, Set, Queue fundamentals + advanced concepts.

Use these questions to build a rock-solid foundation.

---

# 🟦 **JAVA COLLECTIONS PRACTICE WORKBOOK (QUESTIONS ONLY)**

### *100% Practical | Covers List, Set, Queue, Map | Easy → Advanced*

---

# 🔵 **SECTION 1 — LIST (ArrayList / LinkedList)**

### **1. Create an ArrayList of integers and reverse it without using Collections.reverse().**

### **2. Remove all duplicates from a List without using Set or Stream.**

### **3. Given a List<Integer>, shift all zeros to the end while maintaining order.**

### **4. Merge two ArrayLists and remove duplicates (preserve order).**

### **5. Find the second highest number in a List<Integer>.**

### **6. Split a List into sublists of size 3.**

### **7. Find frequency of each element in a List without using Map.**

### **8. Remove all even numbers from a List while iterating safely.**

### **9. Find the longest string in a List<String>.**

### **10. Convert a List<List<Integer>> into a single List<Integer>.**

### **11. Sort a list of custom objects (Employee) by salary, then by name.**

### **12. Convert List<Integer> into int[] and back.**

### **13. Rotate a list by k positions (right rotation).**

### **14. Implement your own ArrayList (basic version) with add/get/remove.**

### **15. Find the first non-repeated element in a List.**

---

# 🔵 **SECTION 2 — SET (HashSet / LinkedHashSet / TreeSet)**

### **16. Remove duplicates from a List using HashSet (order does not matter).**

### **17. Remove duplicates while preserving order using LinkedHashSet.**

### **18. Check if two lists are equal ignoring order and duplicates.**

### **19. Find all common elements between two Sets.**

### **20. Find elements present in Set A but not in Set B.**

### **21. Convert a Set to List and sort it.**

### **22. Generate 10 random unique numbers using Set.**

### **23. Given a list of strings, find the number of unique words.**

### **24. Create a TreeSet of Strings and store them in case-insensitive sorted order.**

### **25. Check if HashSet allows multiple nulls (experiment).**

---

# 🔵 **SECTION 3 — QUEUE (PriorityQueue / ArrayDeque)**

### **26. Implement a Queue using ArrayDeque and perform enqueue/dequeue operations.**

### **27. Reverse a queue using a stack.**

### **28. Reverse a queue using recursion.**

### **29. Use PriorityQueue to find the top 3 highest salaries.**

### **30. Use PriorityQueue to sort integers in descending order.**

### **31. Implement a Stack using ArrayDeque.**

### **32. Implement Queue using two Stacks.**

### **33. Implement Stack using two Queues.**

### **34. Check if parentheses in a string are balanced using Stack (ArrayDeque).**

### **35. Given tasks with priorities, schedule them using PriorityQueue.**

---

# 🔵 **SECTION 4 — MAP (HashMap / LinkedHashMap / TreeMap)**

### **36. Count frequency of each word in a paragraph.**

### **37. Find the first non-repeated character using LinkedHashMap.**

### **38. Sort a map by keys (ascending and descending).**

### **39. Sort a map by values (ascending and descending).**

### **40. Convert HashMap to List of keys sorted alphabetically.**

### **41. Convert HashMap to List of entries sorted by value.**

### **42. Find most frequent element in a list using Map.**

### **43. Group employees by department → Map<String, List<Employee>>.**

### **44. Convert List<Employee> to Map<id, Employee>.**

### **45. Merge two maps into one (handle duplicate keys).**

### **46. Remove all entries where value < 10.**

### **47. Convert Map<String,List<Integer>> into a single List<Integer>.**

### **48. LRU Cache implementation using LinkedHashMap.**

---

# 🔵 **SECTION 5 — ITERATORS (Fail-Fast / Fail-Safe)**

### **49. Iterate over a List and remove odd numbers safely using Iterator.**

### **50. Demonstrate ConcurrentModificationException with ArrayList.**

### **51. Demonstrate fail-safe iteration using CopyOnWriteArrayList.**

### **52. Modify a Map while iterating and show safe removal.**

---

# 🔵 **SECTION 6 — ADVANCED COLLECTIONS**

### **53. Implement your own HashSet (buckets + linked list).**

### **54. Use TreeSet to store employees sorted by salary.**

### **55. Use Comparator to sort TreeSet in descending order.**

### **56. Use TreeMap to store words in alphabetical order.**

### **57. Store student grades in TreeMap and print in reverse order.**

### **58. Create a custom PriorityQueue comparator for complex objects.**

### **59. Implement a min-stack using 2 stacks.**

### **60. Implement a browser history using LinkedList.**

---

# 🔵 **SECTION 7 — REAL-WORLD SCENARIO QUESTIONS**

### **61. You have a list of transactions — group by type and count per type.**

### **62. Find duplicate words in a file using Scanner + Map.**

### **63. Remove all null values from any collection.**

### **64. Convert a list of sentences into a set of unique words.**

### **65. Maintain unique logged-in users preserving order using LinkedHashSet.**

### **66. Design a job scheduler using PriorityQueue.**

### **67. Store employee login timestamps in LinkedList FIFO order.**

### **68. Detect anagrams using frequency map.**

### **69. Detect if two lists are rotations of each other.**

### **70. Implement “recent search history” using ArrayDeque (max size 10).**

---

# 🔵 **SECTION 8 — OUTPUT PREDICTION (INTERVIEW FAVORITES)**

### **71. What is the output of inserting elements into a HashSet?**

### **72. What is the output if you add null twice to HashSet?**

### **73. What happens when you insert custom object into HashSet without hashCode()?**

### **74. What is the output of LinkedHashSet when adding duplicates?**

### **75. What happens when PriorityQueue is printed directly?**

### **76. Order of elements in TreeSet with strings “a”, “A”, “b”, “B”?**

### **77. What happens when modifying a list while iterating with for-each?**

### **78. Predict output of map.put(“A”, 1); map.put(“A”, 2);**

### **79. Output of queue operations: offer, poll, peek.**

### **80. What happens if TreeMap receives a null key?**

---

# 🔵 **SECTION 9 — HIGH-LEVEL DESIGN USING COLLECTIONS**

### **81. Design a leaderboard system using TreeMap (score → players).**

### **82. Design a word-frequency counter like Google Docs.**

### **83. Implement undo/redo system using two stacks (ArrayDeque).**

### **84. Implement a playlist manager using LinkedList.**

### **85. Implement a rate-limiter queue (drop old requests).**

### **86. Implement priority-based email sending using PriorityQueue.**

### **87. Implement a restaurant waiting list using Queue.**

---

# 🟦 **DONE.**

If you want, I can convert this into:

📄 PDF
📝 Markdown file
📘 Mini-book
📂 IntelliJ project with solved answers
🧠 Flashcards

Just tell me!
